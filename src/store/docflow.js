import * as dict from '@lib/store/naf/dict';

export const state = () => ({
  items: [],
});
export const mutations = {};
export const actions = {
  async load() {
    console.log('load not implements!');
  },
};
export const namespaced = true;
